function rand(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

let one = rand(0, 25);
let two = rand(0, 25);
let three = rand(0, 25);

console.log(one, two, three);

let = result;

